<% size = (size || 24) %>
<% color = defined?(unchecked) && unchecked ? "gray" : "green" %>
<% opacity = defined?(unchecked) && unchecked ? "50" : "100" %>
<div class="text-<%= color %>-700 dark:text-<%= color %>-400 opacity-<%= opacity %>">
  <svg xmlns="http://www.w3.org/2000/svg" width="<%= size %>" height="<%= size %>" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-check-circle"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline><title><%= label %></title></svg>
</div>

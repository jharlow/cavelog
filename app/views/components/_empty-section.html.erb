<div class="w-full p-12 flex flex-col items-center min-h-24 border-2 border-dashed dark:border-slate-700 rounded-md">
  <p class="text-slate-600 dark:text-slate-400"><%= message %></p>
  <% if link ||= nil %>
    <%= link_to_if_signed_in link[:text], link[:href], data: { turbo: false } %>
  <% end %>
</div>

<%= form_with url: path, method: :post do |f| %>
  <%= f.label :location_id, location.title %>
  <%= f.hidden_field :location_id, value: location.id %>
  <%= f.submit "#{action} location" %>
<% end %>
